; Sequence literals. Elements are counted in Python rather than captured here:
; an unquantified child pattern yields one match per element, not per container.
(list) @array
(tuple) @array
(set) @array
